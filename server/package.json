{
  "name": "rin-server",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "cf:deploy": "wrangler deploy",
    "dev": "bun wrangler dev --port 11498",
    "dev:cron": "bun wrangler dev --port 11498 --test-scheduled #see https://developers.cloudflare.com/workers/configuration/cron-triggers/#test-cron-triggers",
    "cf-typegen": "wrangler types",
    "db:gen": "bun drizzle-kit generate",
    "preview": "wrangler pages dev dist --compatibility-date=2023-12-20",
    "migrate:visits": "bun run scripts/migrate-visits.ts",
    "test": "bun run test:ci",
    "test:unit": "bun test --preload ./tests/helpers/no-network.ts src tests/security tests/unit --pass-with-no-tests",
    "test:integration": "bun test --preload ./tests/helpers/no-network.ts tests/integration --pass-with-no-tests",
    "test:remote": "bun run scripts/run-remote-tests.ts",
    "test:ci": "bun run test:unit && bun run test:integration",
    "test:coverage": "bun run test:coverage:ci",
    "test:coverage:ci": "bun test --preload ./tests/helpers/no-network.ts --coverage src tests/security tests/unit tests/integration --pass-with-no-tests",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.2",
    "drizzle-kit": "^0.21.2",
    "wrangler": "^4.61.1",
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "typescript": "^5.0.4",
    "strip-indent": "^4.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@rin/api": "workspace:*",
    "arctic": "^3.7.0",
    "aws4fetch": "^1.0.20",
    "drizzle-orm": "^0.30.10",
    "fast-xml-parser": "^5.3.6",
    "feed": "^5.2.0",
    "hono": "^4.10.2",
    "html-to-md": "^0.8.5",
    "jose": "^6.1.3",
    "rehype-stringify": "^10.0.0",
    "remark-gfm": "^4.0.0",
    "remark-parse": "^11.0.0",
    "remark-rehype": "^11.1.0",
    "unified": "^11.0.4"
  },
  "trustedDependencies": [
    "es5-ext"
  ]
}
